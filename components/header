import React, { Component } from 'react';
import { Header } from 'react-native-elements';
import { Icon } from 'react-native-vector-icons/FontAwesome';

export default class AppHeader extends Component {
    render() {
        return (

            <Header
            leftComponent={{ icon: 'chevron-left', onPress: () => console.log('pressed'), color: '#000', size: 40 }}
            centerComponent={{ text: 'header', style: { color: '#000', fontSize: 30, fontWeight: '500' } }}
            containerStyle={{
              backgroundColor: '#fff',
            }}
            />

        );
    }
}
